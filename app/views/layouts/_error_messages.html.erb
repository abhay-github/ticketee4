<% if object.errors.any? %>
	<div class="alert">
		<h3>This form has <%= pluralize(object.errors.count, "error") %></h3>
		<ul>
			<% object.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>	
		</ul>
	</div>
<% end %>