<%= form_for @project do |f|%>
	<%= render 'layouts/error_messages', object: @project %>
	<%= f.label :name %>
	<%= f.text_field  :name%>

	<%= f.label :description %>
	<%= f.text_area :description%>

	<%= f.submit %>
<% end %>