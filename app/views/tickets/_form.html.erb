<%= form_for [@project, @ticket] do |f| %>
	<%= render 'layouts/error_messages', object: @ticket %>

	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :description %>
	<%= f.text_area :description %>

	<%= f.submit %>
<% end %>